[project]
name = "etoro-server"
version = "0.1.0"
description = "MCP server for eToro Public API (read-only)"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.2.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
